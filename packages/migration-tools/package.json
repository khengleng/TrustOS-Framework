{
  "name": "@trustsystem/migration-tools",
  "version": "0.1.0",
  "description": "Database, configuration, template, module and framework migrations, with dry run and rollback.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
