{
  "name": "@trustos/ledger",
  "version": "0.1.0",
  "description": "Double-entry bookkeeping: journals, postings, reversal and trial balance. Posted journals are immutable.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/audit": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/financial-core": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
