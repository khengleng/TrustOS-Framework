{
  "name": "@trustsystem/governance-workflow-bridge",
  "version": "0.1.0",
  "description": "The approval and case experience over the TrustOS workflow framework. Authoritative state stays in the workflow engine.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-tool-core": "0.1.0",
    "@trustsystem/workflow-approvals": "0.1.0",
    "@trustsystem/workflow-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
