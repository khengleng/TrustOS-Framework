{
  "name": "@trustsystem/governance-data-access",
  "version": "0.1.0",
  "description": "Enforces the three data-access classes: approved read-only, API-only mutation, and forbidden.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-resource-policy": "0.1.0",
    "@trustsystem/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
