{
  "name": "@trustos/governance-audit-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Forwards Governance Tool actions into the TrustOS audit trail, enriched with the internal application that caused them.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/audit": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
