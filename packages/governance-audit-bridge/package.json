{
  "name": "@trustsystem/governance-audit-bridge",
  "version": "0.1.0",
  "description": "Forwards Governance Tool actions into the TrustOS audit trail, enriched with the internal application that caused them.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/audit": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
