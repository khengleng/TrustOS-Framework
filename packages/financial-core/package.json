{
  "name": "@trustos/financial-core",
  "version": "0.1.0",
  "private": true,
  "description": "Money, currency, fixed-point decimals, rounding, allocation and financial identifiers. No floating point.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
