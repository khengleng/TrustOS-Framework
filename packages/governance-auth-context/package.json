{
  "name": "@trustsystem/governance-auth-context",
  "version": "0.1.0",
  "description": "Normalizes an authenticated enterprise identity into a TrustOS actor context. Never trusts a client-supplied role or organization.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-tool-core": "0.1.0",
    "@trustsystem/identity": "0.1.0",
    "@trustsystem/rbac": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
