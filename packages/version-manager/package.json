{
  "name": "@trustos/version-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Semantic versioning, ranges, the framework compatibility matrix and upgrade recommendations.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
