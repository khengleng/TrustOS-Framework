{
  "name": "@trustsystem/sre-core",
  "version": "0.1.0",
  "description": "Services, dependencies, runbooks and maintenance windows \u2014 the registry everything else in SRE hangs from.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
