{
  "name": "@trustos/sre-core",
  "version": "0.1.0",
  "private": true,
  "description": "Services, dependencies, runbooks and maintenance windows — the registry everything else in SRE hangs from.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
