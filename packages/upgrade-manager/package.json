{
  "name": "@trustos/upgrade-manager",
  "version": "0.1.0",
  "private": true,
  "description": "The upgrade plan: preflight checks, backup, migration steps, validation, rollback and a report.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/compatibility-engine": "0.1.0",
    "@trustos/dependency-analyzer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/migration-tools": "0.1.0",
    "@trustos/release-manager": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
