{
  "name": "@trustsystem/ai-observability",
  "version": "0.1.0",
  "description": "The AI dashboard: requests, latency, tokens, cost, cache and agent usage.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/ai-sdk": "0.1.0",
    "@trustsystem/cost-monitor": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/observability": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
