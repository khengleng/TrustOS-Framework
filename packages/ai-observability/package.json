{
  "name": "@trustos/ai-observability",
  "version": "0.1.0",
  "private": true,
  "description": "The AI dashboard: requests, latency, tokens, cost, cache and agent usage.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/ai-sdk": "0.1.0",
    "@trustos/cost-monitor": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/observability": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
