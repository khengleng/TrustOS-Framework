{
  "name": "@trustos/dependency-analyzer",
  "version": "0.1.0",
  "description": "Cycles, unused modules, missing dependencies, version conflicts and breaking changes across the module graph.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/module-registry": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
