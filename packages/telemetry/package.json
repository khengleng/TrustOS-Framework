{
  "name": "@trustos/telemetry",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first usage, performance and error signals. Off unless switched on, and never carrying tenant data.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
