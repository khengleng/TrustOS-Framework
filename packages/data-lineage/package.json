{
  "name": "@trustos/data-lineage",
  "version": "0.1.0",
  "private": true,
  "description": "Where governed data came from and where it goes. Queryable; extension interfaces rather than a scanner.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/data-catalog": "0.1.0",
    "@trustos/data-classification": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
