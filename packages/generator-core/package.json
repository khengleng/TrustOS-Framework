{
  "name": "@trustsystem/generator-core",
  "version": "0.1.0",
  "description": "Safe, deterministic, transactional file generation from local TrustOS templates.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/module-registry": "0.1.0",
    "@trustsystem/template-registry": "0.1.0",
    "handlebars": "^4.7.8",
    "zod": "^3.24.1"
  }
}
