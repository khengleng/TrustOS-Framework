{
  "name": "@trustos/service-accounts",
  "version": "0.1.0",
  "description": "Machine identities: service accounts with scoped credentials, client-credentials support, and a hard block on interactive login.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "nest",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/api-keys": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/identity": "0.1.0",
    "@trustos/rbac": "0.1.0",
    "@trustos/security-events": "0.1.0",
    "@trustos/security-policy": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "@nestjs/common": {
      "optional": true
    },
    "@nestjs/core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^11.1.28",
    "@trustos/security-testing": "0.1.0"
  }
}
