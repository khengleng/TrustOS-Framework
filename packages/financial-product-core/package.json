{
  "name": "@trustsystem/financial-product-core",
  "version": "0.1.0",
  "description": "The financial product vocabulary: definition schema, lifecycle, reference data, permissions and execution ports.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/financial-core": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "@trustsystem/workflow-definition": "0.1.0",
    "zod": "^3.24.1"
  }
}
