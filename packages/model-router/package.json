{
  "name": "@trustos/model-router",
  "version": "0.1.0",
  "private": true,
  "description": "Chooses a model from a requirement, with fallback and tenant policy.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/ai-policy": "0.1.0",
    "@trustos/ai-sdk": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/model-registry": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
