{
  "name": "@trustsystem/model-router",
  "version": "0.1.0",
  "description": "Chooses a model from a requirement, with fallback and tenant policy.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/ai-policy": "0.1.0",
    "@trustsystem/ai-sdk": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/logging": "0.1.0",
    "@trustsystem/model-registry": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
