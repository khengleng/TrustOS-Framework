{
  "name": "@trustos/governance-resource-policy",
  "version": "0.1.0",
  "description": "The approved resource registry: what an internal application may reach, under which access class, with whose approval.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
