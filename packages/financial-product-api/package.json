{
  "name": "@trustos/financial-product-api",
  "version": "0.1.0",
  "description": "Stable API exposure for a composed product: route descriptors, OpenAPI, idempotency, rate limiting and the headless dispatcher.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "@trustos/financial-product-registry": "0.1.0",
    "@trustos/financial-product-runtime": "0.1.0",
    "zod": "^3.24.1"
  }
}
