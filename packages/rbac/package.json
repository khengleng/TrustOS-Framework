{
  "name": "@trustos/rbac",
  "version": "0.1.0",
  "description": "Roles, permissions, guards and decorators with deny-by-default routing.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^11.1.28",
    "@trustos/errors": "0.1.0",
    "@trustos/shared-types": "0.1.0"
  }
}
