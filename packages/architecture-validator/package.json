{
  "name": "@trustos/architecture-validator",
  "version": "0.1.0",
  "description": "Layering, naming, dependency direction, folder structure, domain boundaries and security rules.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
