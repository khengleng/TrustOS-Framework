{
  "name": "@trustsystem/financial-reporting",
  "version": "0.1.0",
  "description": "General ledger, trial balance, wallet, transaction, settlement and fee reports, with CSV and Excel.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/accounts": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/financial-core": "0.1.0",
    "@trustsystem/ledger": "0.1.0",
    "@trustsystem/logging": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "@trustsystem/wallet": "0.1.0",
    "zod": "^3.24.1"
  }
}
