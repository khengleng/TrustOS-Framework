{
  "name": "@trustsystem/framework-health",
  "version": "0.1.0",
  "description": "Whether the framework itself is healthy, and what is degrading it.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/dependency-analyzer": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/quality-gates": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
