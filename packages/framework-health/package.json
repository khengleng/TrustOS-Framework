{
  "name": "@trustos/framework-health",
  "version": "0.1.0",
  "private": true,
  "description": "Whether the framework itself is healthy, and what is degrading it.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/dependency-analyzer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/quality-gates": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
