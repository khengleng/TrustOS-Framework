{
  "name": "@trustos/code-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Generates a CRUD slice — model, DTOs, repository, service, controller, tests and documentation — from one declaration.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/architecture-validator": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/validation": "0.1.0",
    "zod": "^3.24.1"
  }
}
