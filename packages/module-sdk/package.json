{
  "name": "@trustsystem/module-sdk",
  "version": "0.1.0",
  "description": "The contract every TrustOS module implements: metadata, lifecycle, configuration, permissions, audit, health and tenant-scoped persistence.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "nest",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/logging": "0.1.0",
    "@trustsystem/observability": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "@trustsystem/tenancy": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@nestjs/common": "^11.1.28",
    "@trustsystem/audit": "0.1.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "@nestjs/common": {
      "optional": true
    }
  }
}
