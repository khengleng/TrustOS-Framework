{
  "name": "@trustos/api-rate-limit",
  "version": "0.1.0",
  "private": true,
  "description": "Rate limits: per second, minute and day, with burst, and the honest statement of what a fixed window is.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/api-consumer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
