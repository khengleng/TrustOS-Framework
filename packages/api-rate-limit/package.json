{
  "name": "@trustsystem/api-rate-limit",
  "version": "0.1.0",
  "description": "Rate limits: per second, minute and day, with burst, and the honest statement of what a fixed window is.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/api-consumer": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
