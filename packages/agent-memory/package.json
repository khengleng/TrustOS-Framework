{
  "name": "@trustsystem/agent-memory",
  "version": "0.1.0",
  "description": "Conversation, session, user, organization and long-term memory with expiry.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/ai-sdk": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/logging": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
