/**
 * @trustsystem/integration-monitor
 *
 * The dashboard behind the health report: throughput, failures, latency and what is currently
 * wrong. Data only — rendering belongs to the application, except for the plain-text form the
 * CLI prints.
 */
export * from './dashboard';
