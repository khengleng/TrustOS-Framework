{
  "name": "@trustsystem/documentation-center",
  "version": "0.1.0",
  "description": "Generates architecture, API, module, CLI and template documentation, the changelog and the dependency graph.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/dependency-analyzer": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/module-registry": "0.1.0",
    "@trustsystem/release-manager": "0.1.0",
    "@trustsystem/template-registry": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
