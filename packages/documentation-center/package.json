{
  "name": "@trustos/documentation-center",
  "version": "0.1.0",
  "private": true,
  "description": "Generates architecture, API, module, CLI and template documentation, the changelog and the dependency graph.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/dependency-analyzer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/module-registry": "0.1.0",
    "@trustos/release-manager": "0.1.0",
    "@trustos/template-registry": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
