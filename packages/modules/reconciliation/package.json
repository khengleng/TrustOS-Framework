{
  "name": "@trustos/module-reconciliation",
  "version": "0.1.0",
  "description": "TrustOS Reconciliation module. Internal and external reconciliation with tolerance rules, an exception queue and resolution history.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "nest",
    "install",
    "README.md",
    "AGENTS.md",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-core": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/module-ledger": "0.1.0",
    "@trustos/module-registry": "0.1.0",
    "@trustos/module-sdk": "0.1.0",
    "@trustos/observability": "0.1.0",
    "@trustos/reconciliation": "0.1.0",
    "@trustos/shared-types": "0.1.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "@nestjs/common": {
      "optional": true
    },
    "@nestjs/core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^11.1.28"
  }
}
