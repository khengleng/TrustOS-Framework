{
  "name": "@trustsystem/module-events",
  "version": "0.1.0",
  "description": "TrustOS Event Bus module. Typed, versioned domain events with a schema registry, ordering per aggregate, retry, dead letters and replay.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "nest",
    "install",
    "README.md",
    "AGENTS.md",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/event-bus": "0.1.0",
    "@trustsystem/event-registry": "0.1.0",
    "@trustsystem/event-sdk": "0.1.0",
    "@trustsystem/logging": "0.1.0",
    "@trustsystem/module-registry": "0.1.0",
    "@trustsystem/module-sdk": "0.1.0",
    "@trustsystem/observability": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "@nestjs/common": {
      "optional": true
    },
    "@nestjs/core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^11.1.28"
  }
}
