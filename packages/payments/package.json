{
  "name": "@trustos/payments",
  "version": "0.1.0",
  "private": true,
  "description": "Payment requests: expiry, status, callbacks, idempotency and provider references. No providers.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/audit": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/financial-core": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "@trustos/transactions": "0.1.0",
    "zod": "^3.24.1"
  }
}
