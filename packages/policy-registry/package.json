{
  "name": "@trustsystem/policy-registry",
  "version": "0.1.0",
  "description": "Versioned, immutable policy documents with their own lifecycle and test cases.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "@trustsystem/workflow-definition": "0.1.0",
    "zod": "^3.24.1"
  }
}
