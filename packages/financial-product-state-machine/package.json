{
  "name": "@trustos/financial-product-state-machine",
  "version": "0.1.0",
  "description": "Deterministic product state machines: the governance lifecycle and the runtime execution states.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
