{
  "name": "@trustos/governance-environment-config",
  "version": "0.1.0",
  "private": true,
  "description": "DEV, UAT and PROD as separate configurations with separate credentials, and the promotion that moves between them.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
