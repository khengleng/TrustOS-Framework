{
  "name": "@trustsystem/governance-environment-config",
  "version": "0.1.0",
  "description": "DEV, UAT and PROD as separate configurations with separate credentials, and the promotion that moves between them.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
