{
  "name": "@trustos/data-retention",
  "version": "0.1.0",
  "private": true,
  "description": "Retention, archival, legal hold and deletion — with the refusal that legal hold always wins.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/data-classification": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
