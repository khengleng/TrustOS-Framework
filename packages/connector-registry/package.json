{
  "name": "@trustos/connector-registry",
  "version": "0.1.0",
  "description": "Provider-neutral connector metadata: interfaces, operations, schemas, timeouts and health. No provider implementations.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "@trustos/provider-sdk": "0.1.0",
    "@trustos/retry": "0.1.0",
    "zod": "^3.24.1"
  }
}
