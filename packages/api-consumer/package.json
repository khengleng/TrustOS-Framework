{
  "name": "@trustsystem/api-consumer",
  "version": "0.1.0",
  "description": "API consumers: scopes, environments, credential metadata and status. Reuses the framework key store.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/api-catalog": "0.1.0",
    "@trustsystem/api-keys": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
