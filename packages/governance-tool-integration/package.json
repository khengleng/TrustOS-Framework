{
  "name": "@trustos/governance-tool-integration",
  "version": "0.1.0",
  "description": "Typed operations against the TrustOS APIs an internal application is allowed to call. No business logic of its own.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/governance-resource-policy": "0.1.0",
    "@trustos/governance-tool-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
