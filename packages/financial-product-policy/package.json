{
  "name": "@trustos/financial-product-policy",
  "version": "0.1.0",
  "description": "Product separation of duties as authorization policies. Every policy can only refuse.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/authorization": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "@trustos/rbac": "0.1.0"
  }
}
