{
  "name": "@trustos/wallet",
  "version": "0.1.0",
  "description": "Ledger-backed wallets: available, held and reserved balances, freeze, holds and history.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/accounts": "0.1.0",
    "@trustos/audit": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/financial-core": "0.1.0",
    "@trustos/ledger": "0.1.0",
    "@trustos/limits": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
