{
  "name": "@trustsystem/analytics",
  "version": "0.1.0",
  "description": "Summaries over collected telemetry: adoption, popularity, error trends.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/telemetry": "0.1.0",
    "zod": "^3.24.1"
  }
}
