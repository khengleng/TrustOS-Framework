{
  "name": "@trustsystem/retry",
  "version": "0.1.0",
  "description": "Retry policies, backoff, jitter, circuit breaker, timeout, cancellation and fallback.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/observability": "0.1.0",
    "zod": "^3.24.1"
  }
}
