{
  "name": "@trustos/financial-product-rules",
  "version": "0.1.0",
  "description": "The deterministic product rules engine: facts, evaluation order, conflict resolution and the explanation trace.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-core": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "@trustos/workflow-definition": "0.1.0",
    "zod": "^3.24.1"
  }
}
