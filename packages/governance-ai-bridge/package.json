{
  "name": "@trustos/governance-ai-bridge",
  "version": "0.1.0",
  "description": "AI assistance for internal tools, through the TrustOS AI Gateway. AI may propose; it may not execute an irreversible action.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/governance-tool-core": "0.1.0",
    "@trustos/human-review": "0.1.0",
    "zod": "^3.24.1"
  }
}
