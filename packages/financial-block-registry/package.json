{
  "name": "@trustsystem/financial-block-registry",
  "version": "0.1.0",
  "description": "The approved catalog of reusable financial blocks: schema, categories, versions and the registry that resolves them.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/financial-product-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
