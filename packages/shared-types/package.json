{
  "name": "@trustos/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-wide types and contracts shared between server and browser code.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
