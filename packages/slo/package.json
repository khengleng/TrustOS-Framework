{
  "name": "@trustos/slo",
  "version": "0.1.0",
  "description": "Objectives and error budgets, with the burn-rate policy that decides what an exhausted budget stops.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/sli": "0.1.0",
    "@trustos/sre-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
