{
  "name": "@trustos/workflow-history",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only workflow history, comments with amendment records, and attachment references.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/audit": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "@trustos/workflow-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
