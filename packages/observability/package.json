{
  "name": "@trustos/observability",
  "version": "0.1.0",
  "description": "Health and readiness probes, request timing, and OpenTelemetry-ready interfaces.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.28",
    "@trustos/config": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/logging": "0.1.0",
    "@trustos/shared-types": "0.1.0"
  }
}
