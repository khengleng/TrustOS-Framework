{
  "name": "@trustsystem/template-registry",
  "version": "0.1.0",
  "description": "Typed, validated catalog of the approved TrustOS application templates.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
