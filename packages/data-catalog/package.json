{
  "name": "@trustos/data-catalog",
  "version": "0.1.0",
  "description": "A searchable catalog of governed data: databases, tables, columns, API fields, events, reports, AI sources.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/data-classification": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
