/**
 * @trustsystem/disaster-recovery
 *
 * DR plans: trigger, decision authority and deputy, procedure, communication, validation,
 * failback and evidence.
 *
 * A DR plan is not a longer runbook — the part that fails is the deciding, not the doing. And the
 * capability statement is written to be quotable in a scorecard without rounding up: a plan
 * exercised as a tabletop is documented, not demonstrated.
 */
export * from './plan';
