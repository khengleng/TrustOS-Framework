{
  "name": "@trustos/developer-access",
  "version": "0.1.0",
  "private": true,
  "description": "Developer portal access: registration, sandbox credentials, key requests and what a portal may never show.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/api-catalog": "0.1.0",
    "@trustos/api-consumer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
