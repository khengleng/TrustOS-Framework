/**
 * @trustsystem/developer-access
 *
 * Registration, sandbox credentials, access requests, and what a portal may never show.
 *
 * The default is invisible: an API appears because something makes it visible, never because
 * nothing hid it. A greyed-out entry for a restricted API is reconnaissance served from the
 * documentation site.
 */
export * from './developer-access';
