{
  "name": "@trustsystem/financial-product-variants",
  "version": "0.1.0",
  "description": "Product variants by controlled override: the allow-list of overridable paths and the resolution that records provenance.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/financial-core": "0.1.0",
    "@trustsystem/financial-product-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
