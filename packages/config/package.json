{
  "name": "@trustsystem/config",
  "version": "0.1.0",
  "description": "Validated, fail-fast application configuration. The only package permitted to read process.env.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    ".env.example",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/shared-types": "0.1.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  }
}
