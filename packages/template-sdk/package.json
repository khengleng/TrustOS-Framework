{
  "name": "@trustos/template-sdk",
  "version": "0.1.0",
  "description": "Headless building blocks every TrustOS industry template shares: navigation, forms, tables, filters, search, pagination, dashboards, charts, CRUD, uploads and notifications.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/validation": "0.1.0",
    "zod": "^3.24.1"
  }
}
