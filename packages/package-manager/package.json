{
  "name": "@trustos/package-manager",
  "version": "0.1.0",
  "description": "Install, update, rollback, dependency resolution, conflict detection and integrity validation. Offline by design.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/dependency-analyzer": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/module-registry": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
