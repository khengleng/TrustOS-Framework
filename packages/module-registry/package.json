{
  "name": "@trustos/module-registry",
  "version": "0.1.0",
  "private": true,
  "description": "The approved TrustOS module catalog and the runtime registry applications discover modules through.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/module-sdk": "0.1.0",
    "zod": "^3.24.1"
  }
}
