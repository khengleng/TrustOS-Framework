{
  "name": "@trustos/financial-product-observability",
  "version": "0.1.0",
  "private": true,
  "description": "The product metric catalog, the collector the runtime reports through and the dashboard descriptors the admin renders.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/financial-product-core": "0.1.0",
    "@trustos/observability": "0.1.0",
    "zod": "^3.24.1"
  }
}
