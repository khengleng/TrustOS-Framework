{
  "name": "@trustsystem/financial-product-observability",
  "version": "0.1.0",
  "description": "The product metric catalog, the collector the runtime reports through and the dashboard descriptors the admin renders.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/financial-product-core": "0.1.0",
    "@trustsystem/observability": "0.1.0",
    "zod": "^3.24.1"
  }
}
