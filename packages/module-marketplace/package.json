{
  "name": "@trustos/module-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Browse, search, rate and verify modules from the local signed catalog. No remote fetch.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/compatibility-engine": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/module-registry": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
