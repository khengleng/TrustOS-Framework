{
  "name": "@trustsystem/continuity",
  "version": "0.1.0",
  "description": "Critical services, RTO and RPO, and the continuity gaps between what is promised and what is proven.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/backup": "0.1.0",
    "@trustsystem/disaster-recovery": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/recovery": "0.1.0",
    "@trustsystem/sre-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
