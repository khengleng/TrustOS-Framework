{
  "name": "@trustos/data-masking",
  "version": "0.1.0",
  "description": "Platform-wide masking, tokenization and pseudonymization policy, and the reveal ceremony at the data layer.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/data-classification": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/governance-pii-policy": "0.1.0",
    "zod": "^3.24.1"
  }
}
