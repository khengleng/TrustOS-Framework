{
  "name": "@trustsystem/data-masking",
  "version": "0.1.0",
  "description": "Platform-wide masking, tokenization and pseudonymization policy, and the reveal ceremony at the data layer.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/data-classification": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/governance-pii-policy": "0.1.0",
    "zod": "^3.24.1"
  }
}
