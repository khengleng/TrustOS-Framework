{
  "name": "@trustsystem/logging",
  "version": "0.1.0",
  "description": "Structured Pino logging with request correlation and mandatory secret redaction.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.28",
    "@trustsystem/config": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
