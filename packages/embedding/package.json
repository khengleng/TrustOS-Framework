{
  "name": "@trustsystem/embedding",
  "version": "0.1.0",
  "description": "Embedding provider abstraction with dimension and metric tracking.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/ai-sdk": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/model-registry": "0.1.0",
    "@trustsystem/provider-sdk": "0.1.0",
    "@trustsystem/retry": "0.1.0",
    "@trustsystem/shared-types": "0.1.0",
    "zod": "^3.24.1"
  }
}
