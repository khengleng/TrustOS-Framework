/**
 * @trustos/api-catalog
 *
 * Which APIs exist, who owns each one, what they return, and what happens to their callers when
 * one is withdrawn.
 *
 * Classification is derived from the operations rather than declared on the API, so an API cannot
 * be labelled below what it actually returns.
 */
export * from './catalog';
