{
  "name": "@trustsystem/api-catalog",
  "version": "0.1.0",
  "description": "The API catalog: owners, versions, classification, lifecycle, consumers and the OpenAPI behind each.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/data-classification": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/sre-core": "0.1.0",
    "@trustsystem/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
