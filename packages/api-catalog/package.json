{
  "name": "@trustos/api-catalog",
  "version": "0.1.0",
  "description": "The API catalog: owners, versions, classification, lifecycle, consumers and the OpenAPI behind each.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/data-classification": "0.1.0",
    "@trustos/errors": "0.1.0",
    "@trustos/sre-core": "0.1.0",
    "@trustos/version-manager": "0.1.0",
    "zod": "^3.24.1"
  }
}
