{
  "name": "@trustos/workflow-definition",
  "version": "0.1.0",
  "description": "The typed workflow definition format, its validator, the restricted condition language and version comparison.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/errors": "0.1.0",
    "@trustos/shared-types": "0.1.0",
    "@trustos/workflow-core": "0.1.0",
    "zod": "^3.24.1"
  }
}
