{
  "name": "@trustos/quality-gates",
  "version": "0.1.0",
  "description": "The gates a change must clear: architecture, security, performance, docs, tests, coverage, lint, format, OpenAPI, configuration, accessibility.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustos/architecture-validator": "0.1.0",
    "@trustos/errors": "0.1.0",
    "zod": "^3.24.1"
  }
}
