{
  "name": "@trustsystem/policy-engine",
  "version": "0.1.0",
  "description": "The centralized decision point: registry plus evaluator plus decision log, defaulting to deny.",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "restricted"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@trustsystem/authorization": "0.1.0",
    "@trustsystem/errors": "0.1.0",
    "@trustsystem/policy-decision-log": "0.1.0",
    "@trustsystem/policy-evaluator": "0.1.0",
    "@trustsystem/policy-registry": "0.1.0",
    "zod": "^3.24.1"
  }
}
