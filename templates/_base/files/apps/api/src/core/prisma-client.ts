/**
 * This application's generated Prisma client.
 *
 * Re-exported from one place so the rest of the code never carries a long
 * relative path to `prisma/generated/`, and so the generation target can move
 * with a single edit.
 *
 * It contains the framework models *and* this product's models, because
 * `prisma/schema/` holds both.
 */
export * from '../../../../prisma/generated/client';
