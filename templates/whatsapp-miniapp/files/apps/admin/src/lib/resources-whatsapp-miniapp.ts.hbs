/**
 * TrustOS WhatsApp Mini App — console screens.
 *
 * One declaration per resource, read by the table, the filters, the search box and the
 * permission checks. The recurring admin-console bug is a field added to the form and forgotten
 * in the table; it cannot happen when there is one list.
 *
 * A column carrying personal data declares a permission, and the *API* projects it away — see
 * visibleColumns in @trustos/template-sdk. A column hidden in the browser is still in the
 * payload.
 */

import type { ResourceDefinition } from '@trustos/template-sdk';
import { WHATSAPP_MINIAPP_PERMISSIONS as P } from '{{packageName}}-product-domain';

export const WHATSAPP_MINIAPP_RESOURCES: ResourceDefinition[] = [
  {
    key: 'whats-app-profiles',
    label: 'WhatsApp profiles',
    singular: 'WhatsApp profile',
    endpoint: '/whats-app-profiles',
    description: "The WhatsApp-specific facts about a mini app user.",
    table: {
      key: 'whats-app-profiles',
      label: 'WhatsApp profiles',
      endpoint: '/whats-app-profiles',
      emptyHint: 'Create one with POST /api/whats-app-profiles.',
      defaultSort: { key: 'createdAt', direction: 'desc' },
      columns: [
        { key: 'miniAppUserId', label: 'User', format: 'reference', sortable: true },
        { key: 'waId', label: 'WhatsApp id', sortable: true },
        { key: 'phone', label: 'Phone', permission: 'whatsappminiapp.pii.read' },
        { key: 'businessAccountRef', label: 'Business account' },
        { key: 'createdAt', label: 'Created', format: 'datetime', sortable: true },
      ],
    },
    permissions: {
      list: P.WHATS_APP_PROFILE_READ.key,
      read: P.WHATS_APP_PROFILE_READ.key,
      create: P.WHATS_APP_PROFILE_CREATE.key,
      update: P.WHATS_APP_PROFILE_UPDATE.key,
    },
  },
];
